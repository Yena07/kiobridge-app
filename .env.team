# npm run dev:team 이 이 파일을 읽는다 (vite --mode team).
#
# 목이 아니라 팀 백엔드로 붙는다. 운영 배포(main)에 컨트롤러가 올라가기 전까지는
# 로컬에서 dev 를 돌려 붙이는 것이 유일한 연동 시험 방법이다.
#
# 비밀값이 아니다. 어느 구현을 쓸지 고르는 스위치일 뿐이라 저장소에 둔다.
# 백엔드 주소는 여기 두지 않는다 — 서버 환경변수(KIOBRIDGE_API_BASE)로만 준다.
VITE_BACKEND=team
