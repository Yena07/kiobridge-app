# 팀원이 macOS/Windows 를 섞어 써도 줄바꿈 때문에 diff 가 튀지 않게 한다.
* text=auto eol=lf
*.png binary
*.jpg binary
*.fig binary

# 팀 백엔드 사본. gradle 래퍼는 줄바꿈이 틀리면 실행이 안 된다.
# jar 를 텍스트로 다루면 파일이 깨진다.
*.jar binary
backend/gradlew text eol=lf
backend/gradlew.bat text eol=crlf
